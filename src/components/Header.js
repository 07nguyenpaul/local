import React from 'react';

const Header = (state) => {
  return (
    <section>
      <h1>local</h1>
      <section className="navBar">
      Navigation
      </section>
    </section>
  );
};

export default Header;
